---
- name: Include the microhoffman.bash_history role
  hosts: all
  any_errors_fatal: true
  tasks:
    - name: Include role
      ansible.builtin.include_role:
        name: microhoffman.bash_history
